# 黄昏考验设计

本 Submod 将《脑叶公司》的三种 WAW 级黄昏考验改写成适合 RimWorld 的高强度阶段考核。玩家开始 `LCOrdeal_Dusk` 后随机抽取琥珀、血色或绿色黄昏；原作不存在紫罗兰和靛蓝色黄昏，因此不添加对应变体。所有本体、生产物和死亡分裂单位都被镇压后才会通过，超过 75,000 Tick 则失败，并进入 60,000 Tick 冷却。

## 三种变体

| 变体 | 原作核心 | RimWorld 实现 |
| --- | --- | --- |
| 琥珀黄昏「食物链」 | 大型蠕虫在走廊间钻地，生成并携带琥珀黎明；承受高额单次伤害后暂时减速 | 按每五名殖民者一只生成 2–5 条大虫；每 1,800 Tick 将名下「新鲜的食物」补充至三只并带队转移到殖民者附近，重新出现时对半径 4 格造成 18 点伤害；单次承受至少 28 点伤害后以 10% 移速持续 60–120 Tick |
| 血色黄昏「绝顶之战」 | 高速滚动碾压；死亡生成血色正午，正午死亡后继续分裂为血色黎明 | 默认生成两只黄昏本体，每 900 Tick 滚动到殖民者附近并造成半径 3 格、20 点伤害；本体死亡生成一只「汁水大合唱」，正午死亡再生成三只「开始欢呼吧！」，全部级联单位均计入目标 |
| 绿色黄昏「前往何方」 | 四座静止工厂周期性生产三只绿色黎明或正午，场上产物上限为十五 | 默认生成四座静止工厂，首次等待 600 Tick，随后每 2,700–3,600 Tick 尝试生产三只单位；黎明与正午默认各有 50% 概率，所有工厂共用十五只存活产物上限，绿色正午保留宕机、电锯和机枪机制 |

## 缩放、派生目标与奖励

- 琥珀黄昏按当前可用殖民者数量缩放；血色和绿色保持原作标志性的固定本体数量。
- 琥珀子代通过所有者 Pawn ID 绑定父体；每次只补充存活数量到上限。父体钻地时会把已生成子代搬运到同一落点附近。
- 血色分裂由死亡补丁触发；每一级新单位都会追加到目标集合并加入新的突击 Lord。
- 绿色工厂持续停止寻路和 Job，只有生产物主动进攻；工厂及其所有存活生产物必须全部被消灭。
- 通过后回收 `12 + 3 × 殖民者数量` 个脑啡肽箱。

## XML 参数

主要平衡参数集中在 `1.6/Defs/CompanyExaminationDefs/DuskOrdeal.xml` 的 `DuskOrdealSettings`：

| 参数组 | 可调整内容 |
| --- | --- |
| `amber*` | 殖民者缩放比、数量上下限、钻地周期和落点范围、黎明生成批量与存活上限、出现伤害、减速伤害阈值与持续时间 |
| `crimson*` | 黄昏本体数量、正午/黎明分裂数量、滚动周期与落点半径、碾压范围/伤害、黎明设施重定向周期 |
| `green*` | 工厂数量、首次生产延迟、生产间隔、每批数量、全局产物上限、正午概率、生成范围，以及正午宕机和电锯参数 |
| `reward*` | 基础脑啡肽奖励及每名殖民者追加量 |
| `presentation*` | 全屏演出的淡入、停留、淡出、输入拦截时长，以及遮罩和色带透明度 |
| `presentations` | 三种黄昏的主题色、开始/成功/失败翻译键和音效 |

考验时限、失败冷却和 Worker 更新频率由 `CompanyExaminationDef` 的 `durationTicks`、`retryCooldownTicks`、`workerTickInterval` 控制。琥珀重度减速倍率位于 `DawnOrdealHediffs.xml` 的 `LCOrdeal_AmberDuskSlowness`。

## 全屏演出

- 开始、镇压成功和镇压失败分别显示主题色横带、状态字样、黄昏名称与 Wiki 引文。
- 默认淡入 0.35 秒、停留 2.25 秒、淡出 0.75 秒；前 1.5 秒拦截 UI 输入。
- 演出按真实时间更新并保存当前变体、阶段和经过时间；读档后继续播放。
- GUI 异常只终止演出层，不会中断考验状态机。

## 发展阶段接入

`StoryDuskGate.xml` 将 `LCStory_InstitutionalCrisis` 的累计指令条件替换为通过 `LCOrdeal_Dusk`，同时保留“至少完成一条高级部门路线”等其他原条件。没有安装本 Submod 时，Story 原 Def 不会被改写。

## 存档契约

- Worker 本身无活动字段，状态统一保存到 `LCAnomalyOrdeals.dusk.*`。
- 保存内容包括变体、地图、目标 ID、琥珀父子归属与减速结束 Tick、血色技能调度、工厂下次生产 Tick、绿色正午宕机时刻。
- 每次 Tick 都会剔除死亡或被其他 Mod 删除的目标；派生单位生成后立即写入目标集合。
- 通过、失败和取消使用同一清理路径，确保重复调用安全。

## 美术边界

黄昏实体使用本模组独立 Race、FleshType 与 PawnKind，不继承原版虫族或机械族。当前版本仅复用 RimWorld 原版虫族和机械体图形、身体结构、音效和通用 AI，通过尺寸与颜色区分黄昏本体和派生单位，不分发《脑叶公司》原始资源。
