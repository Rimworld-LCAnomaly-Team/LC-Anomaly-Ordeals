# 正午考验设计

本 Submod 将《脑叶公司》的四种 HE 级正午考验改写成适合 RimWorld 殖民地规模、地图寻路与存档模型的阶段考核。玩家开始 `LCOrdeal_Noon` 后，每次尝试随机抽取血色、绿色、紫罗兰或靛蓝色正午；原作不存在琥珀色正午，因此不添加该变体。全部本体和死亡后产生的关联目标都被镇压后才会通过，超过 45,000 Tick 仍未完成则失败并进入 45,000 Tick 冷却。

## 四种变体

| 变体 | 原作核心 | RimWorld 实现 |
| --- | --- | --- |
| 血色正午「汁水大合唱」 | 大型血色实体死亡后分裂为三只血色黎明 | 按殖民者人数生成 1–2 只大型近战机械体；每只死亡时默认生成三只「开始欢呼吧！」，新单位立即加入目标集合和突击 Lord，并继承黎明小丑优先破坏收容平台与供电建筑的行为 |
| 绿色正午「理解的过程」 | 同时使用机枪与电锯，行动一段时间后随机宕机 | 生成 2–6 只配备机枪的重型机械体；其近身电锯每 150 Tick 对半径 2.2 格内殖民者造成伤害，每个个体独立在 1,200–1,800 Tick 后停机 240–390 Tick |
| 紫罗兰正午「请给我们爱！！！」 | 石碑坠落造成高额伤害，固定在原地攻击并降低逆卡巴拉计数器 | 生成 2–5 座石碑，落地时对半径 6 格造成 35 点冲击；石碑附加近乎无法移动的 Hediff，每 90 Tick 对半径 5 格释放精神脉冲，并每 1,800 Tick 令随机有效收容目标的计数器降低 1 |
| 靛蓝色正午「清道夫」 | 四组三人进入设施，近战吸血、蓄力攻击并吞食尸体恢复 | 默认生成四组、每组三名清道夫；造成伤害时按 35% 治疗自身，每 750 Tick 对半径 2.5 格释放 18 点精神攻击，每 180 Tick 搜索 5 格内尸体并在吞食后完全恢复 |

## 缩放、目标与奖励

- 血色、绿色和紫罗兰使用 `ceil(殖民者数量 / 每目标人数)` 计算数量，再应用各自上下限；靛蓝色使用固定分组数量和组规模。
- 初始单位从敌对地图入口生成并加入机械体阵营的突击 Lord。紫罗兰石碑不参与寻路，而是在 Worker 每次更新时停止移动和当前 Job。
- 血色正午死亡后生成的黎明小丑会追加到同一考验目标集合，因此击杀本体不会提前结束考验。
- 通过后回收 `8 + 2 × 殖民者数量` 个脑啡肽箱，作为原作正午能源奖励的 RimWorld 化表达。

## XML 参数

主要平衡参数集中在 `1.6/Defs/CompanyExaminationDefs/NoonOrdeal.xml` 的 `NoonOrdealSettings` 中，无需重新编译：

| 参数组 | 可调整内容 |
| --- | --- |
| `crimson*` | 每名正午对应殖民者数、数量上下限、死亡分裂数量、黎明小丑设施重定向周期 |
| `green*` | 每名目标对应殖民者数、数量上下限、独立宕机间隔与持续时间、电锯攻击周期/范围/伤害 |
| `violet*` | 每名石碑对应殖民者数、数量上下限、落地冲击范围/伤害、精神脉冲周期/范围/伤害、计数器降低周期与数值 |
| `indigo*` | 分组数量、每组人数、组内散布、食尸检查周期与搜索范围、蓄力攻击周期/范围/伤害、吸血比例 |
| `reward*` | 基础脑啡肽奖励及每名殖民者追加量 |
| `presentation*` | 全屏演出的淡入、停留、淡出、输入拦截时长，以及遮罩和色带透明度 |
| `presentations` | 每种正午的主题色、开始/成功/失败标题与文本翻译键、对应音效 |

考验总时限、失败冷却和 Worker 更新间隔分别使用 `CompanyExaminationDef` 的 `durationTicks`、`retryCooldownTicks`、`workerTickInterval`。紫罗兰石碑的移动倍率复用 `DawnOrdealHediffs.xml` 中的 `LCOrdeal_VioletNoonImmobile`。

## 全屏演出

- 开始、镇压成功和镇压失败分别触发演出，显示“正午考验”、考验名称和 Wiki 引文。
- 默认淡入 0.35 秒、停留 2.25 秒、淡出 0.75 秒，前 1.5 秒拦截 UI 输入但不强制暂停。
- 演出使用不受游戏倍速影响的真实时间；当前变体、阶段和已播放时间随存档保存。
- 绘制异常会安全关闭演出并记录一次错误，不影响考验 Worker。

## 发展阶段接入

`StoryNoonGate.xml` 将 `LCStory_OrganizationalIntegration` 的阶段条件替换为通过 `LCOrdeal_Noon`。黎明负责前一阶段门槛，正午负责组织整合门槛；没有安装本 Submod 时，LC Anomaly Story 的原条件保持不变。

## 存档契约

- Worker 不持有字段状态；变体、地图 ID、目标 ID 和全局调度 Tick 均保存于 `LCAnomalyOrdeals.noon.*`。
- 绿色个体的下次宕机和恢复时刻按 Pawn ID 保存；血色派生目标会在生成时追加 ID。
- `OnLoaded` 只验证原地图并重连目标，不重复生成单位；通过、失败和取消均按已保存 ID 幂等清理。

## 美术边界

正午实体使用本模组独立 Race、FleshType 与 PawnKind，不继承原版机械族。当前版本仅复用 RimWorld 原版机械体图形、身体结构、音效和通用 AI，通过尺寸与配色区分四种正午，不分发《脑叶公司》原始美术或音频资源。后续可以在保持 Def 名称不变的前提下替换原创素材。
