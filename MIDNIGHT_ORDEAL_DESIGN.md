# 午夜考验设计

本 Submod 将《脑叶公司》的三种标准 ALEPH 级午夜考验改写成 RimWorld 的终盘阶段考核。玩家开始 `LCOrdeal_Midnight` 后随机抽取琥珀、绿色或紫罗兰午夜；惨白午夜属于独立的构筑部终局链，不进入此随机池。所有本体和派生目标都被镇压后才会通过，超过 120,000 Tick 则失败并进入 90,000 Tick 冷却。

## 三种变体

| 变体 | 原作核心 | RimWorld 实现 |
| --- | --- | --- |
| 琥珀午夜「永恒的盛宴」 | 两条巨型蠕虫不断钻地，每次生成两只琥珀黄昏；黄昏继续生成黎明 | 默认生成两只母体，900 Tick 后开始生产，此后每 900–1,200 Tick 各生成两只「食物链」并钻地到殖民者附近；每只母体终生最多生产六只黄昏，击杀子代不会返还额度；黄昏每 1,800 Tick 补充三只黎明至存活上限三只 |
| 绿色午夜「终末螺旋」 | 静止高塔同时发射固定射线与顺时针旋转射线；受击后旋转减速 | 默认生成一座静止高塔，等待 600 Tick 后启动；固定射线角度为 90°，另一条射线默认用 7,200 Tick 旋转一周，每 12 Tick 结算 15 点黑色伤害并减速，转完后冷却 1,800 Tick |
| 紫罗兰午夜「神明的迷思」 | 红、白、黑、苍白四座祭坛吸收对应伤害并使用不同的全设施攻击 | 每种颜色默认生成一座静止祭坛；祭坛完整吸收对应 LC Core 伤害并用该伤害量治疗已有创伤，周期性选择殖民者位置预警并释放四色攻击；生命降至 70%、40%、10% 时追加防卫攻击 |

## 琥珀午夜状态机

- 母体从敌对入口出现，出现和每次钻地落地时以 LC 红色伤害对半径 5 格造成 35 点伤害。
- 每只母体单独保存下一次生产 Tick 与累计生产数。累计数使用终生上限，而不是当前存活数，避免通过击杀黄昏无限刷新生产额度。
- 新生成的黄昏记录母体 ID，并保存自己的黎明生产 Tick。黄昏生成的黎明记录黄昏 ID。
- 母体、黄昏和黎明都进入同一目标集合；只击杀两只母体不会直接通过考验。

## 绿色午夜射线

- 高塔附加近乎无法移动的 Hediff，并在每次 Worker 更新时停止寻路与 Job。
- 射线按“从高塔出发的无限前向射线”计算：殖民者必须位于前方，且到射线中心线的垂直距离不超过 `greenBeamWidthCells`。
- 命中后附加持续 120 Tick、移动速度 50% 的 `LCOrdeal_GreenMidnightBeamSlowness`；重复命中会刷新剩余时间。
- 高塔累计承受 405 点伤害后，旋转速度在 600 Tick 内降为 50%。持续时间可继续叠加，但最多保留 6,000 Tick。
- 射线沿途按每八格生成闪光作为可见提示；视觉采样间距不参与伤害判定。

## 紫罗兰午夜祭坛

| 祭坛 | 吸收伤害 | RimWorld 攻击 |
| --- | --- | --- |
| 红色 | `LC_RedDamage` | 目标点半径 3.5 格内造成 45 点红色伤害 |
| 白色 | `LC_WhiteDamage` | 目标点半径 9 格内造成 18 点白色伤害 |
| 黑色 | `LC_BlackDamage` | 从祭坛穿过目标点的前向直线造成 30 点黑色伤害，线宽 2.5 格 |
| 苍白 | `LC_PaleDamage` | 目标点半径 5 格内造成 12 点苍白伤害 |

- 常规攻击首次延迟 600–900 Tick，之后每 1,500–1,800 Tick 发起一次。
- 目标位置先保存并预警 480 Tick；预警期间每 60 Tick显示闪光，结算时使用保存的位置，不追踪已经移动的殖民者。
- 跨越生命阈值产生的额外攻击进入持久化队列，避免一次高伤跨越多个阈值时覆盖前一轮攻击。
- 对应色伤害在 `Pawn.PreApplyDamage` 前缀中被吸收，因此不会先破坏身体部位或杀死祭坛；剩余治疗量按创伤严重度依次消耗。

## 缩放、目标与奖励

- 三种午夜均使用 XML 中的固定本体数量，默认分别为两只母体、一座高塔和每色一座祭坛。
- 本体使用机械体阵营；可移动的琥珀单位加入不会撤退的突击 Lord，绿色高塔和紫罗兰祭坛保持静止。
- 通过后回收 `15 + 4 × 殖民者数量` 个脑啡肽箱，对应午夜约 25% 能源奖励的终盘强度。

## XML 参数

主要参数集中在 `1.6/Defs/CompanyExaminationDefs/MidnightOrdeal.xml` 的 `MidnightOrdealSettings`：

| 参数组 | 可调整内容 |
| --- | --- |
| `amber*` | 母体数量、首次生产延迟、生产间隔、黄昏批量与终生上限、生成/钻地范围、出现伤害、黄昏行动周期、黎明批量与存活上限 |
| `green*` | 高塔数量、启动延迟、伤害结算周期、整圈旋转时间、冷却、固定射线角度、线宽/伤害/减速、受击减速阈值/持续时间/叠加上限/倍率、视觉采样间距 |
| `violet*` | 每色祭坛数量、首次攻击和后续周期、预警时间、四色伤害与范围/线宽、三段生命阈值和每段追加攻击数 |
| `reward*` | 基础脑啡肽奖励及每名殖民者追加量 |
| `presentation*` | 全屏演出的淡入、停留、淡出、输入拦截时长，以及遮罩和色带透明度 |
| `presentations` | 三种午夜的主题色、开始/成功/失败文本翻译键和音效 |

考验时限、冷却和 Worker 更新间隔由 `durationTicks`、`retryCooldownTicks`、`workerTickInterval` 控制。绿色射线减速倍率和静止效果位于 `DawnOrdealHediffs.xml`。

## 全屏演出

- 开始、镇压成功和镇压失败均显示全屏遮罩、变体色横带、“午夜考验”状态字样、名称和 Wiki 引文。
- 默认淡入 0.35 秒、停留 2.75 秒、淡出 0.85 秒；前 1.75 秒拦截 UI 输入。
- 演出按真实时间更新，当前阶段、变体和经过时间随存档保存；绘制失败时安全关闭演出。

## 发展阶段接入

`StoryMidnightGate.xml` 向 `LCStory_CorporateEndgame` 添加通过 `LCOrdeal_Midnight` 的条件，同时保留公司终局原有工作、脑啡肽和研究条件。惨白系列通过另一条独立节点链接入同一终局。

## 存档契约

- Worker 无活动字段，所有状态位于 `LCAnomalyOrdeals.midnight.*`。
- 保存内容包括变体、地图、目标 ID、琥珀父子归属和累计生成数、绿色角度/冷却/减速/累计受伤、紫罗兰目标位置/结算 Tick/生命阶段/攻击队列。
- 射线角度以千分之一度整数保存，累计伤害以千分之一点整数保存，避免 `ExaminationContext` 仅持久化基础类型造成精度丢失。
- `OnLoaded` 不重新生成目标；通过、失败和取消均按目标 ID 幂等清理。

## 美术边界

当前版本复用 RimWorld 原版虫族与机械体图形，以尺寸、颜色和闪光 Fleck 表达午夜单位与预警，不分发《脑叶公司》原始美术或音频资源。
